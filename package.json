{
  "name": "march-madness-survivor",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^25.3.0",
    "concurrently": "^9.2.1",
    "mastra": "^1.3.2"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "mastra dev",
    "dev:api": "mastra dev",
    "dev:web": "npm --prefix apps/web run dev",
    "dev:all": "concurrently \"npm run dev:api\" \"npm run dev:web\"",
    "build": "mastra build",
    "build:web": "npm --prefix apps/web run build",
    "start": "mastra start",
    "start:web": "npm --prefix apps/web run start",
    "test": "bun test",
    "test:unit": "bun test tests/scoring.test.ts",
    "test:integration": "bun test tests/workflow-rules.test.ts",
    "backtest": "bun run scripts/backtest-survivor.ts"
  },
  "dependencies": {
    "@mastra/core": "^1.5.0",
    "@mastra/loggers": "^1.0.2",
    "@mastra/mcp": "^1.0.1",
    "@mastra/memory": "^1.4.0",
    "@mastra/pg": "^1.5.0",
    "pg": "^8.16.0",
    "zod": "^4"
  }
}
